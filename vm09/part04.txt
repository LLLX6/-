th]');if(b){cloth=b.dataset.cloth;paintAvatar()}};save()}function paintAvatar(){document.documentElement.style.setProperty('--skin',skin);document.documentElement.style.setProperty('--cloth',cloth);$$('[data-skin]').forEach(b=>b.classList.toggle('active',b.dataset.skin===skin));$$('[data-cloth]').forEach(b=>b.classList.toggle('active',b.dataset.cloth===cloth))}
function openAdmin(role){currentAdmin=role;showOnly('admin');$('#adminTitle').textContent=role==='owner'?'OWNER CONTROL CENTER':'MERCHANT STUDIO';renderAdmin()}function renderAdmin(){const id=$('#storePicker').value||stores[0].id,s=stores.find(x=>x.id===id),gross=cart.reduce((a,x)=>a+x.price,0),openCount=stores.filter(x=>x.open).length;if(currentAdmin==='merchant'){$('#adminContent').innerHTML=`<div class="admhero"><div><small>MERCHANT / ${s.unit}</small><h1>${s.name}</h1><p>إدارة متجرك من نفس بيانات عالم المتسوق.</p></div><select class="mini" onchange="document.querySelector('#storePicker').value=this.value;renderAdmin()">${stores.map(x=>`<option value="${x.id}" ${x.id===s.id?'selected':''}>${x.name}</option>`).join('')}</select></div><div class="metrics"><div class="metric"><small>حالة المتجر</small><b>${s.open?'مفتوح':'مغلق'}</b></div><div class="metric"><small>المخزون</small><b>${s.inventory}</b></div><div class="metric"><small>منتجات</small><b>4</b></div><div class="metric"><small>طلبات تجريبية</small><b>${cart.filter(x=>x.store===s.id).length}</b></div></div><div class="grid2"><div class="box"><h3>تشغيل المتجر</h3><div class="row"><div><b>${s.name}</b><small> · ${s.unit}</small></div><button class="toggle" onclick="toggleStore('${s.id}')">${s.open?'إغلاق':'فتح'}</button></div><div class="row"><span>Premium frontage</span><button class="toggle" onclick="togglePremium('${s.id}')">${s.premium?'ON':'OFF'}</button></div></div><div class="box"><h3>المخزون</h3>${products.filter(p=>p.store===s.id).map(p=>`<div class="row"><span>${p.name} · ${p.price.toFixed(3)} ر.ع</span><button class="mini" onclick="adjustInv('${s.id}',1)">+ مخزون</button></div>`).join('')}</div></div>`}else{$('#adminContent').innerHTML=`<div class="admhero"><div><small>SUPER ADMIN / WORLD OPS</small><h1>مالك المول</h1><p>تحكم كامل في الوحدات والمتاجر والتشغيل.</p></div></div><div class="metrics"><div class="metric"><small>الوحدات</small><b>10</b></div><div class="metric"><small>المفتوح</small><b>${openCount}/10</b></div><div class="metric"><small>الإشغال</small><b>100%</b></div><div class="metric"><small>سلة العالم</small><b>${gross.toFixed(3)}</b></div></div><div class="box"><h3>الوحدات الرقمية</h3>${stores.map(s=>`<div class="row"><div><b>${s.unit} · ${s.name}</b><small> — ${s.cat} · مخزون ${s.inventory}</small></div><div><button class="toggle" onclick="togglePremium('${s.id}')">${s.premium?'PREMIUM':'STANDARD'}</button> <button class="toggle" onclick="toggleStore('${s.id}')">${s.open?'OPEN':'CLOSED'}</button></div></div>`).join('')}</div>`}}function toggleStore(id){let s=stores.find(x=>x.id===id);s.open=!s.open;save();renderAdmin()}function togglePremium(id){let s=stores.find(x=>x.id===id);s.premium=!s.premium;save();renderAdmin()}function adjustInv(id,n){stores.find(x=>x.id===id).inventory+=n;save();renderAdmin()}
const canvas=$('#worldCanvas'),ctx=canvas.getContext('2d');function resize(){const d=Math.min(devicePixelRatio||1,2),r=canvas.getBoundingClientRect();canvas.width=r.width*d;canvas.height=r.height*d;ctx.setTransform(d,0,0,d,0,0)}addEventListener('resize',resize);function project(wx,wz){let dx=wx-player.x,dz=wz-player.z,co=Math.cos(-player.yaw),si=Math.sin(-player.yaw),rx=dx*co-dz*si,rz=dx*si+dz*co;let depth=-rz+18;if(depth<1)return null;let w=canvas.clientWidth,h=canvas.clientHeight,horizon=h*.32,scale=Math.min(w*1.10,h*.82)/depth;return{x:w/2+rx*scale,y:horizon+h*.48/depth*12,scale,depth}}function drawMall(){let w=canvas.clientWidth,h=canvas.clientHeight;ctx.clearRect(0,0,w,h);let g=ctx.createLinearGradient(0,0,0,h);g.addColorStop(0,'#101912');g.addColorStop(.42,'#1b211d');g.addColorStop(1,'#6d665c');ctx.fillStyle=g;ctx.fillRect(0,0,w,h);ctx.fillStyle='#cfc6b8';ctx.beginPath();ctx.moveTo(w*.28,h);ctx.lineTo(w*.44,h*.32);ctx.lineTo(w*.56,h*.32);ctx.lineTo(w*.72,h);ctx.closePath();ctx.fill();for(let z=-30;z<45;z+=5){let a=project(-10,z),b=project(10,z);if(a&&b){ctx.strokeStyle='#756f6755';ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}for(let x=-10;x<=10;x+=2.5){let a=project(x,-30),b=project(x,42);if(a&&b){ctx.strokeStyle='#756f6738';ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}let visible=[];for(let s of stores){let p=project(s.x,s.z);if(!p||p.depth>65)continue;visible.push([p,s]);}visible.sort((a,b)=>b[0].depth-a[0].depth);for(let [p,s] of visible)drawStore(p,s);for(let n of npcs){let p=project(n.x,n.z);if(p&&p.depth<55)drawHuman(p,n.skin,n.cloth,false,n.phase)}if(!first)drawPlayer();ctx.fillStyle='#f1dcc0aa';ctx.font='9px system-ui';ctx.textAlign='center';ctx.fillText('ENTRANCE',w/2,h*.36)}function drawStore(p,s){let sc=clamp(p.scale*.65,2.5,70),side=s.x<0?-1:1,width=clamp(7*sc,55,270),height=clamp(4.3*sc,42,180),x=p.x-side*width*.28,y=p.y-height*.96;ctx.fillStyle='#161412';ctx.fillRect(x-width/2,y,width,height);ctx.strokeStyle=s.open?s.color:'#4c4640';ctx.lineWidth=2;ctx.strokeRect(x-width/2,y,width,height);ctx.fillStyle=s.color+'55';ctx.fillRect(x-width*.34,y+height*.45,width*.68,height*.52);ctx.fillStyle='#eee8df';ctx.font=`${clamp(sc*.35,8,18)}px system-ui`;ctx.textAlign='center';ctx.fillText(s.name,x,y+height*.24);ctx.fillStyle=s.open?'#8ed28c':'#b36d67';ctx.font='8px system-ui';ctx.fillText(s.open?'OPEN':'CLOSED',x,y+height*.39)}function drawHuman(p,sk,cl,isPlayer,phase=0){let sc=clamp(p.scale*.075,0.55,1.4),x=p.x,y=p.y,walk=Math.sin(phase)*7*sc;ctx.save();ctx.translate(x,y);ctx.scale(sc,sc);ctx.fillStyle='#0005';ctx.beginPath();ctx.ellipse(0,5,18,6,0,0,6.28);ctx.fill();ctx.strokeStyle='#24272b';ctx.lineWidth=8;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(-6,-32);ctx.lineTo(-8+walk,-3);ctx.mov