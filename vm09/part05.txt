eTo(6,-32);ctx.lineTo(8-walk,-3);ctx.stroke();ctx.strokeStyle=cl;ctx.lineWidth=10;ctx.beginPath();ctx.moveTo(-9,-67);ctx.lineTo(-18-walk*.7,-38);ctx.moveTo(9,-67);ctx.lineTo(18+walk*.7,-38);ctx.stroke();ctx.fillStyle=cl;ctx.beginPath();ctx.roundRect(-17,-78,34,48,12);ctx.fill();ctx.fillStyle=sk;ctx.beginPath();ctx.ellipse(0,-94,15,18,0,0,6.28);ctx.fill();ctx.fillStyle='#241d18';ctx.beginPath();ctx.arc(0,-99,15,Math.PI,6.28);ctx.fill();ctx.fillStyle='#2b2019';ctx.fillRect(-6,-93,2,2);ctx.fillRect(5,-93,2,2);if(isPlayer){ctx.strokeStyle='#e5bd7a';ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(0,8,25,10,0,0,6.28);ctx.stroke();ctx.fillStyle='#f4dfb9';ctx.font='bold 8px system-ui';ctx.textAlign='center';ctx.fillText('PLAYER',0,-119)}ctx.restore()}function drawPlayer(){let w=canvas.clientWidth,h=canvas.clientHeight,phase=performance.now()*.012*clamp(player.speed,0,1);drawHuman({x:w/2,y:h*.82,scale:15},skin,cloth,true,phase)}
function update(dt){let mx=0,my=0;if(keys.has('w')||keys.has('arrowup'))my-=1;if(keys.has('s')||keys.has('arrowdown'))my+=1;if(keys.has('a')||keys.has('arrowleft'))mx-=1;if(keys.has('d')||keys.has('arrowright'))mx+=1;if(Math.hypot(move.x,move.y)>.08){mx+=move.x;my+=move.y}let mag=Math.hypot(mx,my);const sprinting=running||keys.has('shift');let target=mag>.08?(sprinting?7.2:4.1):0;player.speed+=(target-player.speed)*clamp(dt*8,0,1);if(mag>.08){mx/=mag;my/=mag;let f=-my,r=mx,co=Math.cos(player.yaw),si=Math.sin(player.yaw),vx=si*f+co*r,vz=-co*f+si*r;player.x=clamp(player.x+vx*player.speed*dt,-10.5,10.5);player.z=clamp(player.z+vz*player.speed*dt,-29,38)}player.yaw+=look.x*dt*1.9;look.x*=Math.pow(.03,dt);for(let n of npcs){n.phase+=dt*n.v*6;n.z+=n.dir*n.v*dt;if(n.z>34||n.z<-27)n.dir*=-1;n.x+=Math.sin(n.phase*.23)*dt*.13}let near=stores.map(s=>({s,d:Math.hypot(player.x-s.x*.55,player.z-s.z)})).sort((a,b)=>a.d-b.d)[0];if(near&&near.d<3.3&&near.s.open&&!activeStore){activeStore=near.s;openStore(near.s)}if(activeStore&&near?.s!==activeStore&&near?.d>5)activeStore=null;$('#coords').textContent=`X ${player.x.toFixed(1)} · Z ${player.z.toFixed(1)}`;$('#motionState').textContent=player.speed>5?'RUNNING':player.speed>.4?'WALKING':'PLAYER READY';$('#coachText').textContent=matchMedia('(pointer:coarse)').matches?'العصا اليسرى للحركة · اليمنى للنظر · RUN للجري':'WASD / الأسهم للحركة · Shift للجري · اسحب الماوس للنظر'}function loop(t){let dt=Math.min((t-last)/1000,.04);last=t;update(dt);drawMall();raf=requestAnimationFrame(loop)}
function openStore(s){let ps=products.filter(p=>p.store===s.id);$('#storePanel').innerHTML=`<div class="panelhead"><div><small>${s.unit} · ${s.cat}</small><h3>${s.name}</h3></div><button class="close" onclick="closeStore()">×</button></div><div class="products">${ps.map(p=>`<div class="product"><div class="pic">${p.icon}</div><b>${p.name}</b><small>${p.price.toFixed(3)} ر.ع</small><button onclick="addCart('${p.id}')">أضف للسلة</button></div>`).join('')}</div>`;$('#storePanel').classList.remove('hidden');toast('دخلت '+s.name)}function closeStore(){$('#storePanel').classList.add('hidden');activeStore=null;player.z+=2}function addCart(id){let p=products.find(x=>x.id===id);cart.push(p);save();toast('تمت الإضافة للسلة')}function openCart(){let total=cart.reduce((a,p)=>a+p.price,0);$('#storePanel').innerHTML=`<div class="panelhead"><div><small>MASTER CART</small><h3>السلة الموحدة</h3></div><button class="close" onclick="closeStore()">×</button></div>${cart.length?cart.map((p,i)=>`<div class="row"><span>${p.name}</span><b>${p.price.toFixed(3)} ر.ع</b></div>`).join(''):'<p>السلة فارغة.</p>'}<div class="row"><strong>الإجمالي</strong><b>${total.toFixed(3)} ر.ع</b></div>`;$('#storePanel').classList.remove('hidden')}
function bindStick(el,target){let knob=el.querySelector('.knob'),pid=null,tid=null;function set(cx,cy){let r=el.getBoundingClientRect(),x=clamp((cx-(r.left+r.width/2))/(r.width*.34),-1,1),y=clamp((cy-(r.top+r.height/2))/(r.height*.34),-1,1),m=Math.hypot(x,y);if(m>1){x/=m;y/=m}target.x=x;target.y=y;knob.style.transform=`translate(${x*32}px,${y*32}px)`}function end(){target.x=target.y=0;knob.style.transform='';pid=tid=null}el.addEventListener('pointerdown',e=>{pid=e.pointerId;try{el.setPointerCapture?.(pid)}catch{}set(e.clientX,e.clientY);e.preventDefault()});el.addEventListener('pointermove',e=>{if(pid===e.pointerId){set(e.clientX,e.clientY);e.preventDefault()}});el.addEventListener('pointerup',e=>{if(pid===e.pointerId)end()});el.addEventListener('pointercancel',end);el.addEventListener('touchstart',e=>{if(tid!==null)return;let t=e.changedTouches[0];tid=t.identifier;set(t.clientX,t.clientY);e.preventDefault()},{passive:false});el.addEventListener('touchmove',e=>{for(let t of e.changedTouches)if(t.identifier===tid){set(t.clientX,t.clientY);e.preventDefault();break}},{passive:false});el.addEventListener('touchend',e=>{for(let t of e.changedTouches)if(t.identifier===tid){end();e.preventDefault();break}},{passive:false});el.addEventListener('touchcancel',end)}
bindStick($('#moveStick'),move);bindStick($('#lookStick'),look);addEventListener('keydown',e=>{if(!$('#game').classList.contains('hidden')){keys.add(e.key.toLowerCase());if(e.key.toLowerCase()==='r')resetPlayer();if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight',' '].includes(e.key))e.preventDefault()}});addEventListener('keyup',e=>keys.delete(e.key.toLowerCase()));addEventListener('blur',()=>{keys.clear();running=false;$('#runBtn').classList.remove('on')});let dragging=false,lx=0;canvas.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'){dragging=true;lx=e.clientX}});addEventListener('pointerup',()=>dragging=false);canvas.addEventListener('pointermove',e=>{if(dragging){player.yaw+=(e.clientX-lx)*-.006;lx=e.clientX}});$('#runBtn').addEventListener('pointerdown',e=>{running=true;e.currentTarget.classList.add('on');e.preventDefault()});['pointerup','pointercancel','pointerleave'].forEach(x=>$('#runBtn').addEventListener(x,e=>{running=false;e.currentTarget.classList.remove('on')}));$('#runBtn').addEventLis